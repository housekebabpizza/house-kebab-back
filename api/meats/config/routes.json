{
  "routes": [
    {
      "method": "GET",
      "path": "/meats",
      "handler": "meats.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/meats/count",
      "handler": "meats.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/meats/:id",
      "handler": "meats.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/meats",
      "handler": "meats.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/meats/:id",
      "handler": "meats.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/meats/:id",
      "handler": "meats.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
